Гаммирование — это метод симметричного шифрования, 
основанный на использовании псевдослучайной последовательности (называемой гаммой) для преобразования текста.

1. Исходный текст: Это сообщение, которое нужно зашифровать. Например, текст из файла input.txt .

2. Гамма: Последовательность чисел, которая выглядит как случайная, но на самом деле создаётся с использованием определённого алгоритма 
(например, на основе генератора псевдослучайных чисел). Она должна быть такой же длины, как исходный текст.

3. Шифрование: Каждый символ текста представляется числом, затем прибавляется соответствующий элемент гаммы по модулю 𝑛 
(где n — количество символов в алфавите). Это превращает исходный текст в зашифрованный.

4. Расшифровка: Чтобы расшифровать текст, выполняется обратная операция: от каждого элемента зашифрованного текста вычитается соответствующий элемент гаммы
(также по модулю 𝑛).